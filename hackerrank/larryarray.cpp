//
// Created by rishi on 1/17/2020.
//
#include<bits/stdc++.h>
using  namespace std;
int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    cout.tie(NULL);
    int t;
    int n;
    while (t--) {
        cin >> n;
        vector<int> v(n);
        fot(int i = 0;i < n;i++)
        {
            cin >> v[i];
        }5

    }
    vector<int> sort_func(vrctor<int> a, int siz)
    {
        for(int i=0;i<n;i++)
        {
            for(int j=i+1;j<n-1-i;j++)
            {
                if(a[j]>a[j+1])
                {

                }
            }
        }
    }
}
